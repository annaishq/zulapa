# Deploy `website/` to GitHub Pages (static site + optional S3 audio mirror). Lexicon is synced from `src/db.json` first.

name: Deploy GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Sync lexicon for site
        run: cp src/db.json website/db.json

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Generate IPA allowlist
        working-directory: website
        run: node scripts/generate-allowlist.mjs

      - name: Sync audio from S3
        working-directory: website
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          S3_AUDIO_BUCKET: ${{ secrets.S3_AUDIO_BUCKET }}
        run: |
          mkdir -p audio
          if [ -z "${AWS_ACCESS_KEY_ID:-}" ] || [ -z "${AWS_SECRET_ACCESS_KEY:-}" ] \
            || [ -z "${AWS_DEFAULT_REGION:-}" ] || [ -z "${S3_AUDIO_BUCKET:-}" ]; then
            echo "Skipping aws s3 sync (optional: set AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, S3_AUDIO_BUCKET)."
            exit 0
          fi
          aws s3 sync "s3://${S3_AUDIO_BUCKET}/audio/" ./audio/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: website

      - id: deploy
        uses: actions/deploy-pages@v4
